export class Transaction {
  date: Date;
  driverId: string;
  driver: string;
  categoryId: string;
  category: string;
  amount: string;
  document: string;
  initiatedBy: string;
  comment: string;
}
